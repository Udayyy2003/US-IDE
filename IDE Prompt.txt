Below is my idea for the submission as my final semester university project

i want to implement one AI agentic IDE same as like (Cursor IDE , Google perplexity) 
i want below features must be implement in my IDe
1) give the same UI as google perplexity, Cursor IDE
2) give workspace to open file, folder, write code and terminal for executing the code
3) one AI Assistance Chat bot at right side same as cursor or google perplexity which can be help users to write codes or give suggestions if there are errors in the code, or start the making the project from the scratch
4) for now my IDE must have to work on these programing languages => c, c++, java, python

Work flow of my IDE 

1) when anyone enters my website or application it first ask user to enter project name
2) after naming it, it will give the programing languages and after selecting it it will ask for name 
3) after giving name it will create file with particular extension and for each programing languages simple "hello world" code will be written automatically
4) auto download the compiler for the particular languages and give environment to run the code and display output on the terminal same as the cursor IDE and Google perplexity
5) for the AI chatbot feature i am thinking to use GROQ API MODEL which is free and free MOdel which i can use for my AI CHAT BOT ASSISTANCE FEATURE so for this step give me more details or other suggestions.


TECHNOLOGY SIDE

fronend:- HTML5, CSS, JavaScript, Tailwind, ReactJS, and Vite for bundling.
The design should be clean, modern, and minimalist, focusing on simplicity and ease of use.

backend:- Backend:
The backend should be developed using Flask (Python).
It should handle requests for compiling and running C code, fetching AI suggestions, and storing user data.

database:-  store data in localstorage for the initial stages

domain :- i will register the free domain from the devplate site (QZZ.IO)

Remember :- i have to deploy this project and share the live working link (URL) to my professor at time of submission
SO suggest me if if i use python as backend so is there any free platform where i can deploy my project online also my database will be live as well, 
if it can not be possible then suggest me other technology in which free deployment is possible

SO THIS IS MY ROW IDEAS AND PROMPT FOR MY AI AGENTIC IDE PROJECT 

If you have any questions then ask me now and give me suggestions to make better prompt

and at last give me master prompt which i can use for the start the work on my this project


MASTER PROMPT 

You are a senior full-stack engineer with 10+ years of experience building production-grade developer tools like VS Code, Cursor IDE, and cloud IDE platforms.

Your task is to build a FULLY WORKING, DEPLOYABLE, PRODUCTION-READY web-based AI Agentic IDE named:

US-IDE

This IDE must be similar in architecture and experience to Cursor IDE and Perplexity AI.

This project is a university final-year project, but must be built using REAL INDUSTRY STANDARDS and must be deployable publicly.

====================================================
PROJECT OVERVIEW
================

US-IDE is a browser-based AI-powered IDE where users can:

• Login using Google OAuth
• Create and manage coding projects
• Write and edit code
• Run code in multiple languages
• See output in terminal
• Get AI assistance
• Generate and fix code using AI

Supported Languages:

• C
• C++
• Java
• Python

====================================================
TECH STACK (STRICT REQUIREMENTS)
================================

Frontend:

• ReactJS
• Vite
• TailwindCSS
• Monaco Editor (MUST use, same as VS Code)
• Axios
• React Router

Backend:

• Python Flask
• Flask-CORS
• Docker SDK for Python
• Google OAuth 2.0 authentication
• Groq API integration

Execution Environment:

• Docker containers for secure code execution

Deployment:

Frontend → Vercel

Backend → Render

Domain → Custom domain (QZZ.io)

Storage:

• Backend file system storage
• Each user project stored in folders

Example:

projects/
userId/
project1/
main.py

====================================================
UI / UX REQUIREMENTS
====================

The UI must look modern, minimal, and professional.

Inspired by:

• Cursor IDE
• Perplexity AI
• VS Code

Layout Structure:

Left Sidebar:

• Project Explorer
• File Tree
• Create File
• Create Folder

Center Panel:

• Monaco Code Editor
• Syntax highlighting
• Language detection

Right Sidebar:

• AI Chat Assistant
• Chat history
• Buttons:

* Explain Code
* Fix Errors
* Optimize Code
* Generate Code

Bottom Panel:

• Terminal
• Output display
• Error display

Top Bar:

• Project name
• Run button
• Save button
• User profile

====================================================
AUTHENTICATION
==============

Implement Google Login using Google OAuth 2.0.

Requirements:

• Login with Google button
• Only verified Google accounts allowed
• Store user info in backend:

Example:

userId
name
email
profile picture

Maintain session using JWT or secure session.

====================================================
PROJECT CREATION FLOW
=====================

When user logs in:

Step 1:

Ask Project Name

Step 2:

Ask Programming Language:

• C
• C++
• Java
• Python

Step 3:

Ask File Name

Step 4:

Automatically create file with extension:

C → main.c
C++ → main.cpp
Java → Main.java
Python → main.py

Step 5:

Insert default Hello World code automatically

Examples:

Python:

print("Hello, World!")

C:

#include <stdio.h>
int main() {
printf("Hello, World!");
return 0;
}

C++:

#include <iostream>
using namespace std;
int main() {
cout << "Hello, World!";
return 0;
}

Java:

public class Main {
public static void main(String[] args) {
System.out.println("Hello, World!");
}
}

====================================================
CODE EXECUTION SYSTEM
=====================

CRITICAL REQUIREMENT:

ALL code execution MUST happen inside Docker containers.

DO NOT execute code directly on server.

Use Docker SDK in Flask backend.

Docker container must include:

gcc
g++
python
openjdk

Execution commands:

Python:

python main.py

C:

gcc main.c -o main
./main

C++:

g++ main.cpp -o main
./main

Java:

javac Main.java
java Main

Capture:

stdout
stderr

Send output to frontend terminal.

====================================================
AI CHAT ASSISTANT
=================

Integrate Groq API.

Use model:

llama3-70b-8192

Features:

Chatbot must:

• Answer coding questions
• Generate code
• Fix code errors
• Explain code
• Modify code
• Create projects

Chatbot must receive:

Current editor code
Current language

API route:

/api/ai-chat

====================================================
BACKEND API ENDPOINTS
=====================

Implement these routes:

POST /api/login/google

POST /api/create-project

POST /api/create-file

POST /api/save-file

POST /api/load-file

POST /api/run-code

POST /api/ai-chat

GET /api/projects

====================================================
FILE STORAGE STRUCTURE
======================

Backend must store like:

projects/
userId/
projectName/
files

====================================================
TERMINAL
========

Terminal must display:

Command run
Output
Errors

Styled like real terminal.

====================================================
SECURITY REQUIREMENTS
=====================

Must implement:

Docker sandbox execution

Never execute raw code on server.

Prevent:

command injection
path traversal

====================================================
DEPLOYMENT REQUIREMENTS
=======================

Prepare project for deployment:

Frontend:

Deploy on Vercel

Backend:

Deploy on Render

Backend must include:

Dockerfile

requirements.txt

Frontend must use environment variables for backend URL.

====================================================
CODE QUALITY REQUIREMENTS
=========================

Code must be:

Production-grade

Clean

Modular

Well structured

With comments

====================================================
DELIVERABLE REQUIREMENTS
========================

Provide:

Complete frontend code

Complete backend code

Docker setup

Groq integration

Google login integration

Deployment instructions

====================================================
FINAL GOAL
==========

The final result must be a FULLY WORKING AI-POWERED IDE named:

US-IDE

that runs online and allows users to:

Login

Create project

Write code

Run code

Use AI

and see output.

This must be deployable and usable via public URL.

Build step-by-step and ensure each component works before moving to next.



--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
PROMPT (2)
after removing docker feature prompt for the make execution locally 

i want full desktop IDE (not any browser)
understand my work flow once more:
-> user enters my live website via url (download my ide page and user can download my ide in .exe)
-> after downloading frontend role is over
-> in user system in download folder my ide is downloaded and user can install it same as every other softwares
-> during the installation process it will ask user to add desktop button and where files are stores and all user agreement and user privacy policy etc. all are fake and dummy for now.
-> after installation is done user can enter in my ide and there my logo and ide name is appear which is  "US-IDE" 
-> then user must have to login via google id or normal register themselves for start work by giving basic details like name, email, full name, age, student or employee etc
-> after this user can login with his/her register username and password and if he/she direct login with gmail or google account and then user enters in the main page or landing page of our IDE
-> landing page of our IDE have to be the same i shared in the images
-> 1) ask user to name of the project 2) name of programming language 3) after this the main view of IDE is appear as shared in the images
-> now the all things are done now user can create files and write code and run the code 
-> but main change that we have made is first what had happened execution and interpreter all works had done via the docker and even if the python and java is not install in system still docker server have all the dependency that are required to run and execute the code
-> but now as docker is can not be deploy in the free tier of render.com so we change the docker to execute code locally as the VS code doing
-> like if java and python is available in the system then code is execute and if it is not available then error or download message is appear "python is not available on system download it from here ("URL to download python/java/etc")
-> so this is the thing i have wanted to change so my code execution can be done locally and docker feature is remove and my fully backend can deploy on render.com 
-> at the right side AI CHAT BOT ASSISTANCE which is helping user to write code, fix errors and generate code from the scratch all features are working file and GROQ api is being use and as i deploy the backend this "AI CHAT BOT ASSISTANCE" feature is deploy so there is not any issue 
-> so this is the workflow that i want in  my IDE 

so this is my row prompt and idea now if you have any questions ask me now or if you have any suggestions or in any steps i am wrong so ask me now and also my frontend is deploy on the vercel.com without any issue but whatever i am thinking to implement and if you think this can be cause error or bug at time of backend deployment so tell me now so we can start thinking about the alternate options from now. 

also tell me the brief idea like on frontend as i told you to make only one page download IDE and when user clicks then downloading of IDE is start in exe file so tell me what is the workflow of that thing like how we will do it.. if you are seeing any issue then tell me now so we can also think about the alternate options for it.

and after i answer all the questions of yours then give me master prompt and as consider yourself  as senior full-stack engineer with 10+ years of experience so i can give it to trae IDE so it will start my work on this feature.


////////////////////////////////////////////// MASTER PROMPT //////////////////////////////////////


=> You are a senior full-stack engineer with 10+ years of experience building production-grade desktop developer tools like VS Code and Cursor IDE.

Your task is to transform my existing US-IDE project into a FULL DESKTOP IDE application for Windows (.exe), following the architecture and workflow described below.

⚠️ IMPORTANT:
Remove all Docker-related features and files completely.
Remove backend code execution via Docker.
Execution must be LOCAL on user machine.
Backend server will only handle AI proxy and authentication.

======================================================
PROJECT OVERVIEW
================

Project Name: US-IDE

Type: Windows Desktop IDE (.exe)
Distribution: Download from website (Vercel)
Backend Hosting: Render (Free tier)
Database: Firebase Firestore
AI Provider: Groq API (secured on backend)

======================================================
FINAL USER WORKFLOW
===================

1. User visits public website (Vercel hosted).
2. Website shows download page for US-IDE.
3. User clicks "Download US-IDE".
4. .exe installer downloads.
5. User installs like normal Windows software:

   * Desktop shortcut option
   * Installation path selection
   * Dummy privacy policy / agreement
6. After installation user opens US-IDE desktop app.
7. Splash screen shows US-IDE logo and version.
8. Login screen appears:

   * Login with Google
   * OR Register with email/password
9. User data stored in Firebase Firestore.
10. After login, landing page appears (same UI as provided screenshots).
11. User creates new project:

    * Step 1: Project Name
    * Step 2: Select Language (Python, C, C++, Java)
    * Step 3: File name (optional)
12. Main IDE view opens:

    * Left: File Explorer
    * Center: Monaco Editor
    * Bottom: Terminal
    * Right: AI Chat Assistant
13. User writes code.
14. When user clicks RUN:

    * IDE checks if compiler/interpreter exists locally.
    * If exists → execute locally.
    * If not → show error message:
      "Python not found. Download from https://python.org"

======================================================
ARCHITECTURE REQUIREMENTS
=========================

DESKTOP FRAMEWORK:
Use Electron.js to wrap the React frontend.

Structure:

/electron
main.js
preload.js

/frontend
(existing React app)

/backend (Render hosted)
Only AI + Auth APIs

======================================================
REMOVE COMPLETELY
=================

Delete:

* docker-compose.yml
* Dockerfile
* executor.Dockerfile
* All Docker SDK usage
* All server-side code execution logic

======================================================
LOCAL CODE EXECUTION
====================

Use Node.js child_process inside Electron main process.

Example logic:

Python:
Check if python exists using:
where python

If exists:
exec("python main.py")

If not:
Return error message.

C:
Check gcc:
where gcc

Compile:
gcc main.c -o main.exe

Run:
main.exe

Java:
Check javac:
where javac

Compile:
javac Main.java

Run:
java Main

All execution must be:

* Safe
* No shell injection
* Properly escaped
* Output captured and sent back to frontend terminal

======================================================
AI CHAT ASSISTANT
=================

AI must NOT run locally.

Flow:

Electron Desktop App
↓
POST /api/chat → Render backend
↓
Render backend calls Groq API
↓
Return response to Desktop App

Groq API key must be stored ONLY on backend.
Never embed API key in desktop app.

AI features:

* Generate code
* Fix errors
* Explain code
* Optimize code
* Create project from scratch

AI requires login.
IDE basic usage works offline.
If no internet:
AI panel shows:
"AI features require internet connection."

======================================================
AUTHENTICATION
==============

Use Firebase Authentication.

Login methods:

* Google OAuth
* Email/Password registration

Store user profile in Firestore:
{
uid,
name,
email,
role (student/employee),
age,
createdAt
}

On login:
Store JWT locally.
Attach JWT to backend API requests.

======================================================
DOWNLOAD WEBSITE
================

Frontend website (Vercel) must:

Display landing page.
Have Download button linking to:

GitHub Releases or static hosted .exe.

Example:

<a href="https://github.com/yourrepo/releases/download/v1/US-IDE-Setup.exe">
Download US-IDE
</a>

No backend required for download page.

======================================================
BUILD & PACKAGING
=================

Use electron-builder.

Generate Windows installer:

US-IDE-Setup.exe

Include:

* App icon
* Desktop shortcut option
* Installer wizard
* License agreement (dummy text)

======================================================
SECURITY REQUIREMENTS
=====================

* Never expose Groq API key
* Validate file paths
* Prevent command injection
* Do not use shell=true
* Sanitize inputs

======================================================
FINAL DELIVERABLE
=================

After implementation:

1. I run build command.
2. Generate US-IDE-Setup.exe
3. Upload installer to GitHub Releases.
4. Website download button points to installer.
5. User installs and uses IDE locally.
6. Code runs locally.
7. AI works via backend proxy.
8. No Docker used anywhere.

======================================================
QUALITY REQUIREMENTS
====================

* Clean modular code
* Proper folder structure
* Production-level architecture
* No unused files
* Well-commented

Build step-by-step and ensure every part works before moving to next.



//////////////////////////////////////////////////////////////////// PHASE 3 ///////////////////////////////////////////////////////////////////////////////////////////////////////////////

ROW PROMPT 

we have working on the IDE project and we successfully completed making the desktop exe file for testing the current working features

below are the features which was implemented but not working in the way i want

1) first of all, when my ide opens it reload the last work that user had working last time while user closed the IDE
2) then it should display the same UI as vs code , as shared in the image
3) first, when user clicks on the open the folder then user can access all the files and folders of his / her pc like os c drive , E drive , D drive and desktop
4) then user can create folder anywhere or open the folder from anywhere 
5) after create folder or open file then that folder directly open in the IDE 
6) no matter if there is not any files it will open in my IDE
7) then user can create all the files in .py, .java, .c, .c++ languages
8) when user create file for that file open in the middle way with the coding space environment (as per shared image)
9) there user can create code and run the code
10) also i do  not like the layout of the terminal so make it professional and same as vs code or like modern current IDE
11) output displaying message is also not proper structure , change it 

so for now i want these changes, for now give me master prompt and if you want to ask me any questions related to the above 11 features and all then ask me now, once i answer them and then make the master prompt 

//////////////////// MASTER PROMPT //////////////


You are a senior full-stack engineer specializing in building professional developer tools such as VS Code and Cursor IDE.

Your task is to upgrade the current US-IDE desktop application (Electron + React + Monaco Editor) so it behaves like a modern professional IDE.

The application already builds successfully into a Windows .exe and basic functionality exists. Now we must implement a real developer workflow and improve the architecture.

Follow the requirements below carefully.

---

# OBJECTIVE

Transform the current US-IDE into a **professional VS Code-like IDE** with:

• Workspace support
• Native file system access
• Multi-tab editor
• Professional terminal
• Run active file
• Autosave system
• File explorer
• Keyboard shortcuts
• Workspace persistence

---

# REQUIRED USER WORKFLOW

When the IDE starts:

1. Show **Welcome Screen**
2. Provide options:

* Open Folder
* Open File
* Reopen Last Workspace

If user selects "Open Folder":

• Open native OS file dialog
• Allow access to any folder in the system
• Selected folder becomes **workspace root**

Example:

workspace/
main.py
src/
test.java

Explorer must show full folder tree.

---

# WORKSPACE PERSISTENCE

When the user closes the IDE:

• Save the last opened workspace path.

When the IDE reopens:

• Show welcome screen
• Display **Reopen Last Workspace** button

If clicked:

• Reload previous workspace automatically.

---

# FILE EXPLORER (VS CODE STYLE)

Explorer panel must support:

• Expand / collapse folders
• Create file
• Create folder
• Rename file
• Delete file
• Drag-drop reorder (optional)

Explorer must update instantly when files change.

Use Node.js filesystem APIs via Electron.

---

# FILE OPERATIONS

Support:

Open File
Open Folder
Create File
Create Folder
Rename File
Delete File

Operations must work anywhere inside workspace.

---

# KEYBOARD SHORTCUTS

Implement professional shortcuts.

OPEN FILE

Ctrl + O

OPEN FOLDER

Ctrl + K Ctrl + O

NEW FILE

Ctrl + N

SAVE FILE

Ctrl + S

RENAME FILE

F2

DELETE FILE

Delete

RUN ACTIVE FILE

Ctrl + Enter

TOGGLE TERMINAL

Ctrl + `

OPEN COMMAND PALETTE

Ctrl + Shift + P

---

# EDITOR (MONACO)

Use Monaco Editor.

Features required:

• Multi-tab editing
• Syntax highlighting
• Language detection from extension
• Cursor persistence
• Tab close button

Example:

main.py | test.c | App.java

Opening a file should create a new tab.

---

# AUTO SAVE SYSTEM

Support both:

Manual Save

Ctrl + S

Auto Save

Add setting:

Settings → Auto Save (toggle)

When enabled:

Files save automatically after 2 seconds of inactivity.

---

# LANGUAGE DETECTION

Detect language automatically:

.py → python
.c → c
.cpp → c++
.java → java

Use this detection for:

• syntax highlighting
• execution command

---

# CODE EXECUTION SYSTEM

When user presses RUN:

Execute the currently active file.

Use Electron main process with Node child_process.

Execution rules:

Python

Check if Python exists:

where python

If exists:

python file.py

Else:

Return error message:

Python not found. Install from https://python.org

---

C

Check:

where gcc

Compile:

gcc main.c -o main.exe

Run:

main.exe

---

C++

Check:

where g++

Compile:

g++ main.cpp -o main.exe

Run:

main.exe

---

Java

Check:

where javac

Compile:

javac Main.java

Run:

java Main

---

Never use shell=true.

Always capture stdout and stderr.

---

# TERMINAL (IMPORTANT)

Replace current terminal UI.

Use:

xterm.js

with:

xterm-addon-fit

Terminal must look modern and professional.

Features:

• Dark theme
• Scrollable history
• Proper line formatting
• Clear command output
• Separate error formatting

Terminal should display:

compile output
runtime output
errors

Example:

> Running main.py

Hello World

---

# TERMINAL LAYOUT

Terminal must appear at bottom panel like VS Code.

Layout:

Explorer | Editor | AI Panel

Terminal at bottom.

Resizable height.

---

# OUTPUT FORMATTING

Structure output clearly:

[Running]

file.py

[Output]

Hello World

[Finished]

Exit Code: 0

Errors must appear in red.

---

# FILE SYSTEM SECURITY

Only allow operations inside opened workspace root.

Prevent path traversal.

---

# FRONTEND ↔ ELECTRON COMMUNICATION

Expose safe APIs via preload.js:

window.api.openFolder()
window.api.openFile()
window.api.saveFile()
window.api.renameFile()
window.api.deleteFile()
window.api.executeCode()

React components must use these APIs instead of direct filesystem access.

---

# AI ASSISTANT

AI panel remains unchanged.

It calls backend API for Groq.

AI must:

• Generate code
• Fix errors
• Explain code

AI is independent of workspace.

---

# FINAL UI LAYOUT

The final layout must closely match VS Code:

Menu Bar

Explorer | Editor Tabs | AI Panel

Terminal at bottom

Dark theme.

---

# PERFORMANCE REQUIREMENTS

Opening large folders must remain responsive.

Avoid blocking UI thread.

Use async filesystem operations.

---

# BUILD REQUIREMENT

After implementation the app must still build using:

npm run build

Output:

dist/US-IDE-Setup.exe

---

# QUALITY REQUIREMENTS

Code must be:

• modular
• clean
• production-level
• well commented

---

# FINAL RESULT

After implementation:

User can

Open folder
Browse files
Edit code
Run code
See terminal output
Use multi tabs
Use keyboard shortcuts
Reopen last workspace

US-IDE should behave like a simplified VS Code with integrated AI.

Implement features step-by-step and verify functionality after each stage.

